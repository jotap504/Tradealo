{
  "buildCommand": "node apps/web/scripts/gen-firebase-config.mjs && npx pnpm@10 --filter web run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npx pnpm@10 install --frozen-lockfile",
  "framework": "nextjs",
  "github": {
    "enabled": true
  }
}
